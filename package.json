{
  "name": "create-fastify-bun-starter",
  "version": "0.3.0",
  "description": "Scaffold a complete Fastify + Bun environment with minimal dependencies! TypeScript-only, REST/Swagger, CLI support, Wolfi-Docker, cluster, testing, benchmarks and more...",
  "type": "module",
  "scripts": {
    "app": "bun run src/cli.ts",
    "bench:json": "bun --expose-gc script/bench_json_parse.ts",
    "bench:logger": "bun --expose-gc script/bench_logger.js",
    "bench:validator": "bun --expose-gc script/bench_validator.js",
    "build": "docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target prod -t fastify-bun-starter:latest . && docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target scan . && docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target logs --output type=local,dest=./build-logs .",
    "build:grype": "docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target scan .",
    "clean": "bun run rimraf -- -q dist build build-logs coverage node_modules .cache .vitest *.log *.cpuprofile",
    "cluster": "bun run src/cluster.ts",
    "dev": "bun run --hot src/cli.ts",
    "kill": "taskkill /F /IM bun.exe",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "publish:npm": "npm login && npm publish --access public",
    "release": "bun run script/release.ts",
    "rimraf": "bun run script/rimraf.ts",
    "server": "bun run src/cli.ts server",
    "test": "bun run lint && bun run script/run-tests.ts && bun run script/coverage-report.ts",
    "test:verbose": "bun run lint && tsc -p ./tsconfig.verbose.json && bun run script/run-tests.ts --verbose && bun run script/coverage-report.ts",
    "test:ci": "bun test ci",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/helmet": "^12.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@types/node": "^24.9.2",
    "fastify": "^5.2.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@types/bun": "^1.1.6",
    "pino": "^10.1.0",
    "zod": "^4.1.12"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "bin": {
    "create-fastify-bun-starter": "script/create.js"
  },
  "files": [
    "script/create.js",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "fastify",
    "bun",
    "typescript",
    "starter",
    "template",
    "framework",
    "rest-api",
    "swagger",
    "cli",
    "validation",
    "scaffold",
    "generator",
    "cluster",
    "performance",
    "benchmarks",
    "docker"
  ],
  "author": "eram@weblegions.com",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/eram/fastify-bun-starter.git"
  },
  "bugs": {
    "url": "https://github.com/eram/fastify-bun-starter/issues"
  },
  "homepage": "https://github.com/eram/fastify-bun-starter#readme",
  "private": false
}